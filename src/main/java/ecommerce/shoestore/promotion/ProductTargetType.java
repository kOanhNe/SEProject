package ecommerce.shoestore.promotion;

public enum ProductTargetType {
    ALL,       // Áp dụng toàn bộ sản phẩm
    PRODUCT,   // Áp dụng sản phẩm cụ thể (dùng shoeId)
    CATEGORY   // Áp dụng theo danh mục (dùng categoryId)
}
